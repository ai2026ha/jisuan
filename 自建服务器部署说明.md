# 代理计算中心：自建服务器部署

本版本同时兼容 Render 和自建服务器。自建服务器推荐使用：

**Nginx → FastAPI/Uvicorn（1 worker）→ PostgreSQL 16**

这样可以保证多设备同时计算的数据库并发安全，并保持现有 WebSocket 实时同步。

## 1. 服务器要求

推荐 Ubuntu 22.04/24.04、Debian 12 或其他可运行 Docker Compose 的 Linux。

至少安装：

```bash
docker --version
docker compose version
```

## 2. 首次启动

进入项目目录：

```bash
cp .env.example .env
```


### 数据库连接方式

自建服务器的 Docker Compose 不直接拼接 `DATABASE_URL`，而是把数据库参数分别传给应用：

- `DB_HOST`
- `DB_PORT`
- `DB_NAME`
- `DB_USER`
- `DB_PASSWORD`

程序会使用 SQLAlchemy 安全构造 PostgreSQL URL，因此 `POSTGRES_PASSWORD` 中包含 `@`、`/`、`:`、`#` 等特殊字符也不会破坏连接串。Render 部署仍然优先使用 Render 提供的 `DATABASE_URL`。

编辑 `.env`，至少修改：

- `POSTGRES_PASSWORD`
- `SESSION_SECRET`（建议 32 字节以上随机字符串）
- `ADMIN_PASSWORD`

首次先保持：

```text
SESSION_COOKIE_SECURE=0
```

启动：

```bash
docker compose up -d --build
```

查看状态：

```bash
docker compose ps
```

健康检查：

```bash
curl http://127.0.0.1/healthz
```

返回 `{"ok":true,"database":"postgresql"}` 表示应用和数据库均正常。

## 3. 多设备实时同步

`deploy/nginx.conf` 已配置 `/ws` WebSocket 升级头。

当前应用刻意保持 **1 个 Uvicorn worker**。原因是现有实时广播连接表位于应用进程内：

- PostgreSQL：负责多设备同时写入、同一代理并发累计不丢账；
- 单 worker：保证全部 WebSocket 客户端都在同一个广播器内，修改后其他设备立即刷新。

不要自行把 `--workers 1` 改成 2、4 等多个 worker。以后如果确实需要多进程扩容，应先把广播层升级成 Redis Pub/Sub，再增加 worker。

## 4. HTTPS

生产环境建议使用真实域名 + HTTPS。

项目包含：

```text
deploy/nginx.https.example.conf
```

按该文件顶部注释操作：

1. 获取域名证书；
2. 将证书保存为 `deploy/certs/fullchain.pem` 与 `deploy/certs/privkey.pem`；
3. Docker Compose 为 Nginx 增加 443 端口和证书目录挂载；
4. 将 HTTPS 示例配置替换为 `deploy/nginx.conf`；
5. `.env` 改为 `SESSION_COOKIE_SECURE=1`；
6. 执行：

```bash
docker compose up -d --build
```

HTTPS 后浏览器会自动使用 `wss://` 连接 WebSocket。

## 5. 从 Render 迁移 / 网页上传恢复当前有效数据

在旧 Render 项目控制台点击：

**“导出当前数据”**

该备份已经按要求排除：

- 已删除对象（`is_deleted=true`）
- 已清零历史（`cleared=true`）

未清零的历史记录不会因为其关联的代理、游戏或汇率后来被删除而被过滤。

得到 `当前数据_日期时间.zip` 后，在新服务器网页用管理员账号登录，点击：

**“上传数据”**

网页会先执行预览校验并显示六张表的数据数量；只有点击 **“确认覆盖并恢复”** 后才真正写入数据库。恢复使用单个数据库事务，任意一条失败都会整体回滚，不会只恢复一半。

服务器还会检查：

- 必须是本系统导出的 ZIP，并包含 `all_data.json`；
- 备份格式版本必须受支持；
- 六张表与记录 ID 必须有效；
- 备份内不得夹带已删除对象或已清零历史；
- 若保留历史引用了后来已删除的代理/游戏/汇率，恢复时会自动生成隐藏关联占位，不会把已删除对象重新显示出来；
- 至少包含一个管理员账号，防止恢复后无法登录。

恢复成功后当前管理员会退出登录；其他在线设备收到实时通知后也会要求重新登录。随后使用备份中的账号登录即可。

> “上传数据”会覆盖目标数据库中的账号、代理、游戏、汇率、计算记录和人工调整。执行前建议先把目标服务器当前数据也导出一份。

### 命令行备用恢复方式

如果备份过大、浏览器上传不方便，仍可使用命令行脚本。先把 ZIP 放进容器：

```bash
docker cp 当前数据_日期时间.zip $(docker compose ps -q app):/app/当前数据.zip
```

再执行：

```bash
docker compose exec -T app python scripts/import_current_data.py /app/当前数据.zip --replace
```

完成后：

```bash
docker compose restart app
```

网页上传默认 ZIP 上限为 100 MB，`all_data.json` 解压后上限为 300 MB；可在 `.env` 通过 `BACKUP_UPLOAD_MAX_MB` 和 `BACKUP_JSON_MAX_MB` 调整。Nginx 已将请求体上限设为 110 MB；如果把 `BACKUP_UPLOAD_MAX_MB` 调到 100 MB 以上，也要同步提高 `deploy/nginx.conf`（以及 HTTPS 示例配置）里的 `client_max_body_size`。

## 6. 日常备份

管理员可以继续使用网页中的 **“导出当前数据”** 保存当前有效业务数据。

PostgreSQL 数据本身保存在 Docker 命名卷 `postgres_data` 中。升级源码时正常执行：

```bash
docker compose up -d --build
```

不会删除该卷，也不会清空数据库。

绝对不要在不确认的情况下执行：

```bash
docker compose down -v
```

`-v` 会删除 PostgreSQL 数据卷。

## 7. 更新源码

替换源码后执行：

```bash
docker compose up -d --build
```

数据库仍连接原来的 `postgres_data` 卷，现有数据继续保留。

## 8. Render 兼容

原来的 `render.yaml` 保留，所以这份源码仍可继续部署到 Render。程序按以下顺序选择数据库：

1. 有 `DATABASE_URL`：使用它（Render 默认方式）；
2. 没有 `DATABASE_URL`、但有 `DB_HOST`：使用自建 PostgreSQL；
3. 两者都没有：才回退到本地 `calculator.db` SQLite。

## 历史记录合并导入（不覆盖当前数据）

管理员可在“历史记录”页面使用 **合并导入历史**，上传本系统“导出历史表”生成的 CSV。

该功能与“覆盖恢复数据”完全不同：

- 只补充缺失的历史计算记录；
- 不删除数据库中已经存在的记录；
- 不修改账号、当前代理、游戏、汇率和人工调整；
- 不修改任何代理当前累计金额；
- 重复上传同一批历史会自动去重；
- 如果来源记录 ID 与现有新记录冲突，会保留现有记录，并把旧历史作为新的历史记录追加；
- 合并导入的旧历史只用于历史查询/导出，不进入“当前计算笔数”和当前代理账单；
- 删除合并导入的旧历史时，也不会从当前代理余额中扣款。

整机迁移、需要让目标数据库完全回到备份状态时，才使用控制台的 **覆盖恢复数据**。
